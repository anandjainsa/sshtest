pipeline {
    agent none
       environment {
       BUILD_DISPLAY_NAME = 'anand'
            }
    stages {
            stage('test') {
            steps {
                sh ''' echo "hello" '''
            }
        }
    }
}

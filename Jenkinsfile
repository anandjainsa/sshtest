pipeline {
    agent none
    stages {
            stage('test') {
            steps {
                build.displayName = 'This build needs help!!!'
                buildDescription "Executed @ jain"
                 }
            }
        }
    }

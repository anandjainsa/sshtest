pipeline {
    agent none
    stages {
            stage('test') {
            steps {
                currentBuild.displayName = 'This build needs help!!!'
                buildDescription "Executed @ jain"
                 }
            }
        }
    }

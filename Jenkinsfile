pipeline {
       environment {
       BUILD_DISPLAY_NAME = 'anand'
            }
    stages {
       tage('test') {
            steps {
                sh ''' echo "hello" '''
            }
        }
    }
}
